# Defects

| ID | Severity | Reproduction | Expected | Observed | Evidence | Owner | Status |
|---|---|---|---|---|---|---|---|
| FCT-TA-001 | Release-blocking | Run `assignTruckType` with otherwise valid freight using `pieces.unit="cm"` and `weight.unit="kg"`. | Stop as `needs_review` without truck or DAT equipment because units are not canonical inches/pounds. | Initially assigned Cargo Van/Van. After repair, returns `needs_review`, reason `AMBIGUOUS_UNITS`, with no truck or DAT equipment. | `qa/truck-assignment.qa.js`; `qa/runs/2026-08-11-truck-assignment-v1/summary.md` | Playwright builder | Resolved; independently verified 2026-08-11 |
| FCT-TA-002 | Release-blocking | Run `assignTruckType` with otherwise valid freight and `temperatureControl={minC:'', maxC:''}`. | Stop as `needs_review` with `TEMPERATURE_CONTROL_INCOMPLETE`. | Initially coerced empty bounds to zero and assigned Reefer Cargo Van/Reefer. After repair, returns `needs_review` with `TEMPERATURE_CONTROL_INCOMPLETE`. | `qa/truck-assignment.qa.js`; `qa/runs/2026-08-11-truck-assignment-v1/summary.md` | Playwright builder | Resolved; independently verified 2026-08-11 |
| FCT-SL-001 | Release-blocking | Load a saved shipment, edit a fingerprinted lane/date field in the CRM without saving, then inspect the Search Loads approval state. | The approval control is disabled until every fingerprinted visible field matches the saved shipment snapshot used by the endpoint. | Initially only saved DAT equipment and a nonempty editor pickup date were checked, so unsaved origin/destination/date edits could leave approval enabled for a different stored snapshot. The repair compares pickup city/state/ZIP/date, delivery city/state/ZIP, and DAT equipment with the saved shipment. | `client/src/pages/EmailQuoteInboxPage.js`; page ESLint and optimized build; `qa/runs/2026-08-13-search-loads-v1/summary.md` | Playwright builder | Resolved; independently verified 2026-08-13 |
| FCT-SL-002 | Release-blocking | Pass the server a ranked offer with `displayedTotal="$1,000"` and `totalUsd=9999`. | Reject because the numeric value must be the exact parse of the displayed total-dollar Rate. | Initially accepted. The repaired validator parses `displayedTotal`, requires equality with `totalUsd`, and also enforces duplicate-ID and equal-rate source-order integrity. The original reproduction now rejects with `ranked offer is invalid`. | `server/services/datRateViewJobs.ts`; `server/scripts/test-dat-search-loads.ts`; `qa/runs/2026-08-13-search-loads-v1/summary.md` | Playwright builder | Resolved; independently verified 2026-08-13 |
| FCT-SL-003 | Release-blocking | Validate an otherwise approved Search Loads request with pickup date `2026-02-31`. | Reject before browser use because the pickup date is not a real calendar date. | Initially accepted by shape-only validation. The repaired validator round-trips the UTC calendar date; the original reproduction and new automation unit case now reject it. | `automation/src/validation.ts`; `automation/tests/validation.test.ts`; `qa/runs/2026-08-13-search-loads-v1/summary.md` | Playwright builder | Resolved; independently verified 2026-08-13 |

No open deterministic defects remain for Search Loads v1 or the truck-assignment v1 extension. Release remains blocked by missing authorized live reliability evidence, not by an unresolved implementation defect.
